[tool.ruff]
target-version = "py314"
line-length = 88
extend-exclude = [
    ".git",
    ".venv",
    ".pytest_cache",
    ".ruff_cache",
    "django_app/**/migrations/*.py",
]

[tool.ruff.lint]
select = [
    "F",
    "I",
]

[tool.bandit]
exclude_dirs = [
    "django_app/*/migrations",
]
